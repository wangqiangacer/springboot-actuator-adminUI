#通过如下配置监控所有的端点
management:
  endpoints:
    web:
      exposure:
        include: "*"
spring:
  profiles:
    active: test
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/test
    password: root
    username: root
#  boot:
#    admin:
#      client:
#        url: "http://localhost:6666"
info:
  name: wangqiang
